name: Setup

description: Setup

runs:
  using: composite

  steps:
    - name: setup node
      uses: actions/setup-node@v6
      with:
        node-version: 24
        cache: npm

    - name: install dependencies
      shell: bash
      run: npm ci
